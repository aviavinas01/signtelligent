flask==3.0.0
flask-cors==4.0.0
mediapipe==0.10.9
opencv-python==4.9.0.80
scikit-learn==1.4.0
numpy==1.26.4
pandas==2.2.0
joblib==1.3.2
Pillow==10.2.0
gunicorn==21.2.0

# ── LSTM / sequence model ──────────────────────────────────────────────────────
# TensorFlow CPU-only (no GPU required). On Apple Silicon use tensorflow-macos.
tensorflow==2.16.1
# Uncomment below instead if on Apple Silicon (M1/M2/M3):
# tensorflow-macos==2.16.1

# Optional: nicer training progress bars
tqdm==4.66.2